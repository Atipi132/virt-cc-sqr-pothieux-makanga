# Local .terraform directories6
TD/2_terraform/foundation-gcp/.terraform/


# .tfstate files
TD/2_terraform/foundation-gcp/*.tfstate
TD/2_terraform/foundation-gcp/*.tfstate.*

# Crash log files
TD/2_terraform/foundation-gcp/crash.log
TD/2_terraform/foundation-gcp/crash.*.log

# Exclude all .tfvars files, which are likely to contain sensitive data, such as
# password, private keys, and other secrets. These should not be part of version 
# control as they are data points which are potentially sensitive and subject 
# to change depending on the environment.
TD/2_terraform/foundation-gcp/*.tfvars
TD/2_terraform/foundation-gcp/*.tfvars.json

# Ignore override files as they are usually used to override resources locally and so
# are not checked in
TD/2_terraform/foundation-gcp/override.tf
TD/2_terraform/foundation-gcp/override.tf.json
TD/2_terraform/foundation-gcp/*_override.tf
TD/2_terraform/foundation-gcp/*_override.tf.json

# Ignore transient lock info files created by terraform apply
TD/2_terraform/foundation-gcp/.terraform.tfstate.lock.info

# Include override files you do wish to add to version control using negated pattern
# !example_override.tf

# Include tfplan files to ignore the plan output of command: terraform plan -out=tfplan
# example: *tfplan*

# Ignore CLI configuration files
TD/2_terraform/foundation-gcp/.terraformrc
TD/2_terraform/foundation-gcp/terraform.rc
