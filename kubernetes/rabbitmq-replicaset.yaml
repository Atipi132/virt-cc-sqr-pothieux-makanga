apiVersion: apps/v1
kind: ReplicaSet
metadata:
  labels:
    app: broker
    soft: rabbitmq
  name: rabbitmq-replicaset
spec:
  replicas: 1                     
  selector:
    matchLabels:
      app: broker
      soft: rabbitmq
  template:                   
    metadata:
      labels:
        app: broker
        soft: rabbitmq
    spec:
      containers:
      - name: rabbitmq
        image: rabbitmq:3.12.0-management
        ports:
        - containerPort: 5672
        resources:
          requests:
            cpu: "4m"
            memory: "32Mi"
